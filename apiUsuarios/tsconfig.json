{
  "compilerOptions": {
    "target": "es6",                           // Versión de ECMAScript que deseas utilizar (ES6 o superior).
    "module": "commonjs",                      // Sistema de módulos (usa "ESNext" si trabajas con módulos ES en lugar de CommonJS).
    "strict": true,                            // Habilita el modo estricto de TypeScript para mejores chequeos.
    "esModuleInterop": true,                   // Permite interoperabilidad con módulos CommonJS y ESModules.
    "skipLibCheck": true,                      // Omite la verificación de tipos en archivos de definición de bibliotecas.
    "forceConsistentCasingInFileNames": true,  // Asegura que los nombres de archivos se usen de manera consistente.
    "outDir": "./dist",                        // Directorio de salida para los archivos compilados.
    "rootDir": "./src",                        // Directorio raíz de tu código fuente TypeScript.
    "moduleResolution": "node",                // Método de resolución de módulos (usualmente "node" para proyectos Node.js).
    "allowSyntheticDefaultImports": true,      // Permite importar módulos CommonJS como si tuvieran una exportación por defecto.
    "resolveJsonModule": true,                 // Permite la importación de archivos JSON.
    "noEmitOnError": true                      // No emitir archivos si hay errores de compilación.
  },
  "include": ["src"],                           // Carpeta que contiene tus archivos TypeScript.
  "exclude": ["node_modules", "dist"]           // Carpeta que se excluirá del proceso de compilación.
}
